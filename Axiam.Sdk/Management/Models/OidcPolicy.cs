// <auto-generated>
// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
// </auto-generated>

#nullable enable

using System.Text.Json;
using System.Text.Json.Serialization;

namespace Axiam.Sdk.Management.Models;

/// <summary>
/// OpenID Connect surface controls (X7 G8, plan §4.6/§4.8). Two settings that are not password
/// rules, and are here because this is the org-baseline-plus-tenant-override surface every
/// other per-tenant control lives on. They are also the two settings in this model that are
/// *not* of the same kind as each other, so it is worth saying which is which: *
/// [<c>Self::sensitive_scopes_enabled</c>] **is** ordered. Releasing personal data is the
/// less-restrictive direction, so it is validated disable-only — the mirror image of
/// <c>mfa_enforced</c> — and a tenant can turn its organization's decision off but never on. *
/// [<c>Self::default_locale</c>] is **not** ordered, and no ordering is invented for it. A
/// language is a presentation preference; there is no sense in which Italian is stricter than
/// French. [<c>validate_tenant_override</c>] therefore does not check it and
/// [<c>clamp_overrides_to_org</c>] never clears it. The model's rule is "a tenant may only be
/// more restrictive", which binds every field that *has* a restrictiveness; a field that has
/// none cannot violate it.
/// </summary>
public sealed record OidcPolicy
{
    /// <summary>
    /// The BCP 47 tag the sign-in page falls back to when the relying party's <c>ui_locales</c>
    /// selects nothing (W5's chain, plan §4.6). <c>None</c> means "no tenant preference", which
    /// lands on the deployment default (<c>en</c>) — the behaviour every deployment had before
    /// this field existed. A tag this build does not ship also lands there: the parse is exact
    /// rather than a language lookup, so a stored <c>fr-CA</c> reads as "somebody wrote
    /// something this binary does not ship" rather than as a guess at French. Stored as a
    /// string rather than as the <c>Locale</c> enum because that enum lives in
    /// <c>axiam-oauth2</c>, four layers above this crate, and the crate layering points inward.
    /// </summary>
    [JsonPropertyName("default_locale")]
    public string? DefaultLocale { get; init; }

    /// <summary>
    /// Whether <c>address</c> and <c>phone</c> may be registered on a client, requested at the
    /// authorization endpoint, and released at UserInfo (X7 G8). **Off unless an organization
    /// turns it on.** The two scopes release a postal address and a telephone number —
    /// categories of personal data AXIAM has no other use for — so the deployment that has
    /// never thought about them releases nothing, and the operator who has thought about them
    /// says so once, at the organization level, where the lawful basis for holding the data was
    /// decided. The switch is a *capability*, not a grant: with it on, a client still has to
    /// register the scope, the request still has to ask for it, and the user still has to have
    /// consented. It is the first of four gates, and it is the only one an operator can close
    /// for everybody at once.
    /// </summary>
    [JsonPropertyName("sensitive_scopes_enabled")]
    public required bool SensitiveScopesEnabled { get; init; }
}
