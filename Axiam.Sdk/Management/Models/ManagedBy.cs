// <auto-generated>
// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
// </auto-generated>

#nullable enable

using Axiam.Sdk.Management;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Axiam.Sdk.Management.Models;

/// <summary>
/// Who created a client registration (D5, T21.4). The discriminator that separates a
/// registration an administrator made from one that arrived over an open endpoint. Three things
/// read it and each would otherwise have to infer provenance from something that is not
/// provenance: * <c>axiam_oauth2::fapi</c> refuses a FAPI profile on anything but
/// [<c>Admin</c>](Self::Admin) (I5) — a client nobody vetted cannot be financial-grade; * the
/// authorization endpoint forces a consent hop for every other value (D4) — an unrelated party
/// gets a question put to the end user, whatever scopes it asked for; * the T21.4 sweeper
/// deletes only [<c>Dcr</c>](Self::Dcr) rows, so an administrator's client is never swept
/// however long it sits unused. [<c>Admin</c>](Self::Admin) is the serde default and therefore
/// what every row written before T21.4 decodes to, which is the truth: they were all created
/// through <c>POST /oauth2-clients</c> by somebody holding <c>oauth2_clients:create</c>.
/// </summary>
/// <remarks>
/// <para>
/// Each member carries the spelling the server uses on the wire via WireName, so the C# name
/// can follow C# conventions without changing what is sent.
/// </para>
/// <para>
/// An <b>open</b> enum. A value this SDK’s copy of the spec does not list decodes to
/// <c>ManagedBy.Unknown</c> rather than failing the response it arrived in (&#167;27.11 rule 1)
/// — a closed enum turns the next value the server adds into a parse error on the whole list,
/// taking down every record on the page over one field of one of them. A <c>switch</c> over
/// these members needs an <c>Unknown</c> arm.
/// </para>
/// </remarks>
[JsonConverter(typeof(WireEnumConverter<ManagedBy>))]
public enum ManagedBy
{
    /// <summary>The server's <c>admin</c> value.</summary>
    [WireName("admin")]
    Admin,

    /// <summary>The server's <c>dcr</c> value.</summary>
    [WireName("dcr")]
    Dcr,

    /// <summary>The server's <c>cimd</c> value.</summary>
    [WireName("cimd")]
    Cimd,

    /// <summary>
    /// A value this SDK’s copy of the spec does not list.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Its wire spelling is the empty string, which no server value is: carrying an
    /// unrecognised value back into an update is refused by the server rather than silently
    /// written as a spelling it never used. Fifteen of these enums appear in request bodies,
    /// which is what makes that distinction worth having.
    /// </para>
    /// <para>
    /// It is deliberately <b>not</b> the zero member — defaulting to it would make “the server
    /// said nothing” and “the server said something new” the same value.
    /// </para>
    /// </remarks>
    [WireName("")]
    Unknown,
}
