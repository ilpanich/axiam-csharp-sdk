// <auto-generated>
// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
// </auto-generated>

#nullable enable

using Axiam.Sdk.Management;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Axiam.Sdk.Management.Models;

/// <summary>
/// Whether a grant permits an action or refuses it (B1, deny-override). # Precedence Default
/// deny -&gt; an [<c>PermissionEffect::Allow</c>] grant permits -&gt; a
/// [<c>PermissionEffect::Deny</c>] grant refuses, **and beats every allow**, wherever either
/// sits in the resource hierarchy. Deny wins; there is no most-specific-wins tie-break. That
/// choice is deliberate and is argued in full in <c>claude_dev/deny-override-design.md</c>
/// §2.1. The short version: deny-override buys one checkable property — **adding a deny rule
/// can never widen access, and can never be undone by adding allows** — and most-specific-wins
/// buys expressiveness at the cost of making "is X denied?" unanswerable without enumerating
/// every other rule that might out-specify it. [<c>PermissionEffect::Allow</c>] is the default,
/// so data written before this existed, and clients that send no <c>effect</c>, both mean
/// "allow". No migration.
/// </summary>
/// <remarks>
/// <para>
/// Each member carries the spelling the server uses on the wire via WireName, so the C# name
/// can follow C# conventions without changing what is sent.
/// </para>
/// <para>
/// An <b>open</b> enum. A value this SDK’s copy of the spec does not list decodes to
/// <c>PermissionEffect.Unknown</c> rather than failing the response it arrived in (&#167;27.11
/// rule 1) — a closed enum turns the next value the server adds into a parse error on the whole
/// list, taking down every record on the page over one field of one of them. A <c>switch</c>
/// over these members needs an <c>Unknown</c> arm.
/// </para>
/// </remarks>
[JsonConverter(typeof(WireEnumConverter<PermissionEffect>))]
public enum PermissionEffect
{
    /// <summary>The server's <c>allow</c> value.</summary>
    [WireName("allow")]
    Allow,

    /// <summary>The server's <c>deny</c> value.</summary>
    [WireName("deny")]
    Deny,

    /// <summary>
    /// A value this SDK’s copy of the spec does not list.
    /// </summary>
    /// <remarks>
    /// <para>
    /// Its wire spelling is the empty string, which no server value is: carrying an
    /// unrecognised value back into an update is refused by the server rather than silently
    /// written as a spelling it never used. Fifteen of these enums appear in request bodies,
    /// which is what makes that distinction worth having.
    /// </para>
    /// <para>
    /// It is deliberately <b>not</b> the zero member — defaulting to it would make “the server
    /// said nothing” and “the server said something new” the same value.
    /// </para>
    /// </remarks>
    [WireName("")]
    Unknown,
}
