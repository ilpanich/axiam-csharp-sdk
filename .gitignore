# Build outputs
bin/
obj/

# Grpc.Tools-generated gRPC/protobuf stubs land under obj/ at build time (D-05) —
# never committed; the compiled stub classes ship inside the published .nupkg instead.

# NuGet packages produced by `dotnet pack`
*.nupkg
*.snupkg

# IDE / editor
.vs/
*.user
